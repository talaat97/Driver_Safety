import 'package:flutter/material.dart';

class EditOfContactBuilder extends StatelessWidget {
  const EditOfContactBuilder({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
